{
  "component_info": {
    "name": "GitHubDiscussionsLoader",
    "component_type": "document_loader",
    "module_path": "packages.haive-core.src.haive.core.engine.document.loaders.specific.web_github_enhanced",
    "class_name": "GitHubDiscussionsLoader",
    "description": "Custom loader for GitHub Discussions.",
    "source_code": "class GitHubDiscussionsLoader(BaseLoader):\n    \"\"\"Custom loader for GitHub Discussions.\"\"\"\n\n    def __init__(\n        self,\n        repo_url: str,\n        discussion_number: Optional[int] = None,\n        category: Optional[str] = None,\n        answered_only: bool = False,\n        max_discussions: int = 100,\n        include_comments: bool = True,\n        access_token: Optional[str] = None,\n    ):\n        self.repo_url = repo_url\n        self.discussion_number = discussion_number\n        self.category = category\n        self.answered_only = answered_only\n        self.max_discussions = max_discussions\n        self.include_comments = include_comments\n        self.access_token = access_token\n\n        # Parse repo info\n        parsed = urlparse(repo_url)\n        path_parts = parsed.path.strip(\"/\").split(\"/\")\n        if len(path_parts) >= 2:\n            self.owner = path_parts[0]\n            self.repo = path_parts[1]\n        else:\n            raise ValueError(f\"Invalid GitHub URL: {repo_url}\")\n\n    def load(self) -> List[Document]:\n        \"\"\"Load GitHub discussions.\"\"\"\n        try:\n            import requests\n\n            documents = []\n\n            # GitHub GraphQL endpoint\n            url = \"https://api.github.com/graphql\"\n            headers = {\n                \"Authorization\": (\n                    f\"bearer {self.access_token}\" if self.access_token else \"\"\n                ),\n                \"Accept\": \"application/vnd.github.v3+json\",\n            }\n\n            # GraphQL query for discussions\n            query = \"\"\"\n            query($owner: String!, $repo: String!, $first: Int!) {\n                repository(owner: $owner, name: $repo) {\n                    discussions(first: $first) {\n                        nodes {\n                            title\n                            body\n                            number\n                            category { name }\n                            isAnswered\n                            createdAt\n                            author { login }\n                            comments(first: 10) {\n                                nodes {\n                                    body\n                                    author { login }\n                                    createdAt\n                                }\n                            }\n                        }\n                    }\n                }\n            }\n            \"\"\"\n\n            variables = {\n                \"owner\": self.owner,\n                \"repo\": self.repo,\n                \"first\": self.max_discussions,\n            }\n\n            response = requests.post(\n                url,\n                json={\"query\": query, \"variables\": variables},\n                headers=headers,\n            )\n\n            if response.status_code == 200:\n                data = response.json()\n                discussions = (\n                    data.get(\"data\", {})\n                    .get(\"repository\", {})\n                    .get(\"discussions\", {})\n                    .get(\"nodes\", [])\n                )\n\n                for discussion in discussions:\n                    if self.answered_only and not discussion.get(\"isAnswered\"):\n                        continue\n                    if (\n                        self.category\n                        and discussion.get(\"category\", {}).get(\"name\") != self.category\n                    ):\n                        continue\n                    if (\n                        self.discussion_number\n                        and discussion.get(\"number\") != self.discussion_number\n                    ):\n                        continue\n\n                    content = f\"# {discussion['title']}\\n\\n\"\n                    content += f\"**Author:** {discussion['author']['login']}\\n\"\n                    content += f\"**Category:** {discussion['category']['name']}\\n\"\n                    content += f\"**Created:** {discussion['createdAt']}\\n\"\n                    content += f\"**Answered:** {discussion['isAnswered']}\\n\\n\"\n                    content += discussion[\"body\"]\n\n                    if self.include_comments and discussion.get(\"comments\", {}).get(\n                        \"nodes\"\n                    ):\n                        content += \"\\n\\n## Comments\\n\\n\"\n                        for comment in discussion[\"comments\"][\"nodes\"]:\n                            content += f\"**{comment['author']['login']}** ({comment['createdAt']}):\\n\"\n                            content += f\"{comment['body']}\\n\\n\"\n\n                    metadata = {\n                        \"source\": f\"{self.repo_url}/discussions/{discussion['number']}\",\n                        \"type\": \"github_discussion\",\n                        \"number\": discussion[\"number\"],\n                        \"category\": discussion[\"category\"][\"name\"],\n                        \"is_answered\": discussion[\"isAnswered\"],\n                        \"created_at\": discussion[\"createdAt\"],\n                        \"author\": discussion[\"author\"][\"login\"],\n                    }\n\n                    documents.append(Document(page_content=content, metadata=metadata))\n\n            return documents\n\n        except Exception as e:\n            logger.error(f\"Failed to load GitHub discussions: {e}\")\n            return []\n",
    "env_vars": [],
    "timestamp": "2025-07-10T19:45:40.981088",
    "schema": {
      "properties": {
        "repo_url": {
          "type": "<class 'str'>",
          "default": null
        },
        "discussion_number": {
          "type": "typing.Optional[int]",
          "default": null
        },
        "category": {
          "type": "typing.Optional[str]",
          "default": null
        },
        "answered_only": {
          "type": "<class 'bool'>",
          "default": "False"
        },
        "max_discussions": {
          "type": "<class 'int'>",
          "default": "100"
        },
        "include_comments": {
          "type": "<class 'bool'>",
          "default": "True"
        },
        "access_token": {
          "type": "typing.Optional[str]",
          "default": null
        }
      }
    },
    "metadata": {},
    "has_tool": true,
    "tool_name": "load_documents_githubdiscussionsloader",
    "tool_description": "Load documents using GitHubDiscussionsLoader: Custom loader for GitHub Discussions."
  },
  "document": {
    "page_content": "# GitHubDiscussionsLoader\n**Type:** document_loader\n**Module:** packages.haive-core.src.haive.core.engine.document.loaders.specific.web_github_enhanced\n**Class:** GitHubDiscussionsLoader\n\n## Description\nCustom loader for GitHub Discussions.\n\n## Schema\n```json\n{\n  \"properties\": {\n    \"repo_url\": {\n      \"type\": \"<class 'str'>\",\n      \"default\": null\n    },\n    \"discussion_number\": {\n      \"type\": \"typing.Optional[int]\",\n      \"default\": null\n    },\n    \"category\": {\n      \"type\": \"typing.Optional[str]\",\n      \"default\": null\n    },\n    \"answered_only\": {\n      \"type\": \"<class 'bool'>\",\n      \"default\": \"False\"\n    },\n    \"max_discussions\": {\n      \"type\": \"<class 'int'>\",\n      \"default\": \"100\"\n    },\n    \"include_comments\": {\n      \"type\": \"<class 'bool'>\",\n      \"default\": \"True\"\n    },\n    \"access_token\": {\n      \"type\": \"typing.Optional[str]\",\n      \"default\": null\n    }\n  }\n}\n```\n\n## Usage as Tool\n**Tool Name:** `load_documents_githubdiscussionsloader`\n**Description:** Load documents using GitHubDiscussionsLoader: Custom loader for GitHub Discussions.\n\n## Source Code\n```python\nclass GitHubDiscussionsLoader(BaseLoader):\n    \"\"\"Custom loader for GitHub Discussions.\"\"\"\n\n    def __init__(\n        self,\n        repo_url: str,\n        discussion_number: Optional[int] = None,\n        category: Optional[str] = None,\n        answered_only: bool = False,\n        max_discussions: int = 100,\n        include_comments: bool = True,\n        access_token: Optional[str] = None,\n    ):\n        self.repo_url = repo_url\n        self.discussion_number = discussion_number\n        self.category = category\n        self.answered_only = answered_only\n        self.max_discussions = max_discussions\n        self.include_comments = include_comments\n        self.access_token = access_token\n\n        # Parse repo info\n        parsed = urlparse(repo_url)\n        path_parts = parsed.path.strip(\"/\").split(\"/\")\n        if len(path_parts) >= 2:\n            self.owner = path_parts[0]\n            self.repo = path_parts[1]\n        else:\n            raise ValueError(f\"Invalid GitHub URL: {repo_url}\")\n\n    def load(self) -> List[Document]:\n        \"\"\"Load GitHub discussions.\"\"\"\n        try:\n            import requests\n\n            documents = []\n\n            # GitHub GraphQL endpoint\n            url = \"https://api.github.com/graphql\"\n            headers = {\n                \"Authorization\": (\n                    f\"bearer {self.access_token}\" if self.access_token else \"\"\n                ),\n                \"Accept\": \"application/vnd.github.v3+json\",\n            }\n\n            # GraphQL query for discussions\n            query = \"\"\"\n            query($owner: String!, $repo: String!, $first: Int!) {\n                repository(owner: $owner, name: $repo) {\n                    discussions(first: $first) {\n                        nodes {\n                            title\n                            body\n                            number\n                            category { name }\n                            isAnswered\n                            createdAt\n                            author { login }\n                            comments(first: 10) {\n                                nodes {\n                                    body\n                                    author { login }\n                                    createdAt\n                                }\n                            }\n                        }\n                    }\n                }\n            }\n            \"\"\"\n\n            variables = {\n                \"owner\": self.owner,\n                \"repo\": self.repo,\n                \"first\": self.max_discussions,\n            }\n\n            response = requests.post(\n                url,\n                json={\"query\": query, \"variables\": variables},\n                headers=headers,\n            )\n\n            if response.status_code == 200:\n                data = response.json()\n                discussions = (\n                    data.get(\"data\", {})\n                    .get(\"repository\", {})\n                    .get(\"discussions\", {})\n                    .get(\"nodes\", [])\n                )\n\n                for discussion in discussions:\n                    if self.answered_only and not discussion.get(\"isAnswered\"):\n                        continue\n                    if (\n                        self.category\n                        and discussion.get(\"category\", {}).get(\"name\") != self.category\n                    ):\n                        continue\n                    if (\n                        self.discussion_number\n                        and discussion.get(\"number\") != self.discussion_number\n                    ):\n                        continue\n\n                    content = f\"# {discussion['title']}\\n\\n\"\n                    content += f\"**Author:** {discussion['author']['login']}\\n\"\n                    content += f\"**Category:** {discussion['category']['name']}\\n\"\n                    content += f\"**Created:** {discussion['createdAt']}\\n\"\n                    content += f\"**Answered:** {discussion['isAnswered']}\\n\\n\"\n                    content += discussion[\"body\"]\n\n                    if self.include_comments and discussion.get(\"comments\", {}).get(\n                        \"nodes\"\n                    ):\n                        content += \"\\n\\n## Comments\\n\\n\"\n                        for comment in discussion[\"comments\"][\"nodes\"]:\n                            content += f\"**{comment['author']['login']}** ({comment['createdAt']}):\\n\"\n                            content += f\"{comment['body']}\\n\\n\"\n\n                    metadata = {\n                        \"source\": f\"{self.repo_url}/discussions/{discussion['number']}\",\n                        \"type\": \"github_discussion\",\n                        \"number\": discussion[\"number\"],\n                        \"category\": discussion[\"category\"][\"name\"],\n                        \"is_answered\": discussion[\"isAnswered\"],\n                        \"created_at\": discussion[\"createdAt\"],\n                        \"author\": discussion[\"author\"][\"login\"],\n                    }\n\n                    documents.append(Document(page_content=content, metadata=metadata))\n\n            return documents\n\n        except Exception as e:\n            logger.error(f\"Failed to load GitHub discussions: {e}\")\n            return []\n\n```",
    "metadata": {
      "name": "GitHubDiscussionsLoader",
      "component_type": "document_loader",
      "module_path": "packages.haive-core.src.haive.core.engine.document.loaders.specific.web_github_enhanced",
      "class_name": "GitHubDiscussionsLoader",
      "has_tool": true,
      "has_engine_config": false,
      "env_vars": [],
      "timestamp": "2025-07-10T19:45:40.981088",
      "category": "engines"
    }
  },
  "tool_available": true,
  "engine_config_available": false
}
