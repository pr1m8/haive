{
  "component_info": {
    "name": "GitHubGistsLoader",
    "component_type": "document_loader",
    "module_path": "packages.haive-core.src.haive.core.engine.document.loaders.specific.web_github_enhanced",
    "class_name": "GitHubGistsLoader",
    "description": "Custom loader for GitHub Gists.",
    "source_code": "class GitHubGistsLoader(BaseLoader):\n    \"\"\"Custom loader for GitHub Gists.\"\"\"\n\n    def __init__(\n        self,\n        username: Optional[str] = None,\n        gist_id: Optional[str] = None,\n        public_only: bool = True,\n        max_gists: int = 100,\n        access_token: Optional[str] = None,\n    ):\n        self.username = username\n        self.gist_id = gist_id\n        self.public_only = public_only\n        self.max_gists = max_gists\n        self.access_token = access_token\n\n    def load(self) -> List[Document]:\n        \"\"\"Load GitHub gists.\"\"\"\n        try:\n            import requests\n\n            documents = []\n            headers = {\"Accept\": \"application/vnd.github.v3+json\"}\n            if self.access_token:\n                headers[\"Authorization\"] = f\"token {self.access_token}\"\n\n            if self.gist_id:\n                # Load specific gist\n                url = f\"https://api.github.com/gists/{self.gist_id}\"\n                response = requests.get(url, headers=headers)\n\n                if response.status_code == 200:\n                    gist = response.json()\n                    documents.extend(self._process_gist(gist))\n\n            elif self.username:\n                # Load user's gists\n                url = f\"https://api.github.com/users/{self.username}/gists\"\n                params = {\"per_page\": min(self.max_gists, 100)}\n\n                response = requests.get(url, headers=headers, params=params)\n\n                if response.status_code == 200:\n                    gists = response.json()\n                    for gist in gists[: self.max_gists]:\n                        if self.public_only and not gist.get(\"public\"):\n                            continue\n                        documents.extend(self._process_gist(gist))\n\n            return documents\n\n        except Exception as e:\n            logger.error(f\"Failed to load GitHub gists: {e}\")\n            return []\n\n    def _process_gist(self, gist: Dict[str, Any]) -> List[Document]:\n        \"\"\"Process a single gist into documents.\"\"\"\n        documents = []\n\n        for filename, file_info in gist.get(\"files\", {}).items():\n            content = f\"# Gist: {gist['description'] or 'Untitled'}\\n\"\n            content += f\"**File:** {filename}\\n\"\n            content += f\"**Language:** {file_info.get('language', 'Unknown')}\\n\"\n            content += f\"**Size:** {file_info.get('size', 0)} bytes\\n\\n\"\n            content += \"```\" + (file_info.get(\"language\", \"\").lower() or \"\") + \"\\n\"\n            content += file_info.get(\"content\", \"\")\n            content += \"\\n```\"\n\n            metadata = {\n                \"source\": gist[\"html_url\"],\n                \"type\": \"github_gist\",\n                \"gist_id\": gist[\"id\"],\n                \"filename\": filename,\n                \"language\": file_info.get(\"language\"),\n                \"size\": file_info.get(\"size\"),\n                \"public\": gist.get(\"public\", True),\n                \"created_at\": gist.get(\"created_at\"),\n                \"updated_at\": gist.get(\"updated_at\"),\n            }\n\n            documents.append(Document(page_content=content, metadata=metadata))\n\n        return documents\n",
    "env_vars": [],
    "timestamp": "2025-07-10T19:45:40.997934",
    "schema": {
      "properties": {
        "username": {
          "type": "typing.Optional[str]",
          "default": null
        },
        "gist_id": {
          "type": "typing.Optional[str]",
          "default": null
        },
        "public_only": {
          "type": "<class 'bool'>",
          "default": "True"
        },
        "max_gists": {
          "type": "<class 'int'>",
          "default": "100"
        },
        "access_token": {
          "type": "typing.Optional[str]",
          "default": null
        }
      }
    },
    "metadata": {},
    "has_tool": true,
    "tool_name": "load_documents_githubgistsloader",
    "tool_description": "Load documents using GitHubGistsLoader: Custom loader for GitHub Gists."
  },
  "document": {
    "page_content": "# GitHubGistsLoader\n**Type:** document_loader\n**Module:** packages.haive-core.src.haive.core.engine.document.loaders.specific.web_github_enhanced\n**Class:** GitHubGistsLoader\n\n## Description\nCustom loader for GitHub Gists.\n\n## Schema\n```json\n{\n  \"properties\": {\n    \"username\": {\n      \"type\": \"typing.Optional[str]\",\n      \"default\": null\n    },\n    \"gist_id\": {\n      \"type\": \"typing.Optional[str]\",\n      \"default\": null\n    },\n    \"public_only\": {\n      \"type\": \"<class 'bool'>\",\n      \"default\": \"True\"\n    },\n    \"max_gists\": {\n      \"type\": \"<class 'int'>\",\n      \"default\": \"100\"\n    },\n    \"access_token\": {\n      \"type\": \"typing.Optional[str]\",\n      \"default\": null\n    }\n  }\n}\n```\n\n## Usage as Tool\n**Tool Name:** `load_documents_githubgistsloader`\n**Description:** Load documents using GitHubGistsLoader: Custom loader for GitHub Gists.\n\n## Source Code\n```python\nclass GitHubGistsLoader(BaseLoader):\n    \"\"\"Custom loader for GitHub Gists.\"\"\"\n\n    def __init__(\n        self,\n        username: Optional[str] = None,\n        gist_id: Optional[str] = None,\n        public_only: bool = True,\n        max_gists: int = 100,\n        access_token: Optional[str] = None,\n    ):\n        self.username = username\n        self.gist_id = gist_id\n        self.public_only = public_only\n        self.max_gists = max_gists\n        self.access_token = access_token\n\n    def load(self) -> List[Document]:\n        \"\"\"Load GitHub gists.\"\"\"\n        try:\n            import requests\n\n            documents = []\n            headers = {\"Accept\": \"application/vnd.github.v3+json\"}\n            if self.access_token:\n                headers[\"Authorization\"] = f\"token {self.access_token}\"\n\n            if self.gist_id:\n                # Load specific gist\n                url = f\"https://api.github.com/gists/{self.gist_id}\"\n                response = requests.get(url, headers=headers)\n\n                if response.status_code == 200:\n                    gist = response.json()\n                    documents.extend(self._process_gist(gist))\n\n            elif self.username:\n                # Load user's gists\n                url = f\"https://api.github.com/users/{self.username}/gists\"\n                params = {\"per_page\": min(self.max_gists, 100)}\n\n                response = requests.get(url, headers=headers, params=params)\n\n                if response.status_code == 200:\n                    gists = response.json()\n                    for gist in gists[: self.max_gists]:\n                        if self.public_only and not gist.get(\"public\"):\n                            continue\n                        documents.extend(self._process_gist(gist))\n\n            return documents\n\n        except Exception as e:\n            logger.error(f\"Failed to load GitHub gists: {e}\")\n            return []\n\n    def _process_gist(self, gist: Dict[str, Any]) -> List[Document]:\n        \"\"\"Process a single gist into documents.\"\"\"\n        documents = []\n\n        for filename, file_info in gist.get(\"files\", {}).items():\n            content = f\"# Gist: {gist['description'] or 'Untitled'}\\n\"\n            content += f\"**File:** {filename}\\n\"\n            content += f\"**Language:** {file_info.get('language', 'Unknown')}\\n\"\n            content += f\"**Size:** {file_info.get('size', 0)} bytes\\n\\n\"\n            content += \"```\" + (file_info.get(\"language\", \"\").lower() or \"\") + \"\\n\"\n            content += file_info.get(\"content\", \"\")\n            content += \"\\n```\"\n\n            metadata = {\n                \"source\": gist[\"html_url\"],\n                \"type\": \"github_gist\",\n                \"gist_id\": gist[\"id\"],\n                \"filename\": filename,\n                \"language\": file_info.get(\"language\"),\n                \"size\": file_info.get(\"size\"),\n                \"public\": gist.get(\"public\", True),\n                \"created_at\": gist.get(\"created_at\"),\n                \"updated_at\": gist.get(\"updated_at\"),\n            }\n\n            documents.append(Document(page_content=content, metadata=metadata))\n\n        return documents\n\n```",
    "metadata": {
      "name": "GitHubGistsLoader",
      "component_type": "document_loader",
      "module_path": "packages.haive-core.src.haive.core.engine.document.loaders.specific.web_github_enhanced",
      "class_name": "GitHubGistsLoader",
      "has_tool": true,
      "has_engine_config": false,
      "env_vars": [],
      "timestamp": "2025-07-10T19:45:40.997934",
      "category": "engines"
    }
  },
  "tool_available": true,
  "engine_config_available": false
}
