# Documentation Issues Report

## Log Locations

### 1. **Package Test Logs**

- `/packages/logs/` - Main test logs directory
  - `pytest.log` - Latest pytest output
  - `report.html` - HTML test report
  - `runs/` - Timestamped test runs

### 2. **Build Logs**

- `/tmp/docs_fast.log` - Documentation build log
- `/tmp/docs_rebuild.log` - Latest rebuild log
- `/tmp/haive_docs_build.log` - Background build logs

## Current Issues

### 1. **Sidebar Display Issues** ✅ FIXED

- **Problem**: Module paths showing as titles (e.g., `haive.agents.base`)
- **Solution**: Updated `sidebar-fix.js` to:
  - Convert `haive.agents` → "Agents"
  - Convert `haive.agents.base` → "Base Agent"
  - Convert `haive.agents.base.Agent` → "Agent (base)"
  - Add small module indicators for context

### 2. **Missing Document Titles** (12,448 warnings)

- **Main culprits**:
  - `/guides/core_concepts.rst` - Missing proper title
  - `/guides/architecture.rst` - Missing proper title
  - `/guides/state_management.rst` - Missing proper title
  - `/guides/engine_system.rst` - Missing proper title
  - `/guides/building_agents.rst` - Missing proper title
  - `/guides/agent_patterns.rst` - Missing proper title
  - `/guides/custom_agents.rst` - Missing proper title
  - `/guides/agent_visualization.rst` - Missing proper title
  - `/beta_status.rst` - Missing entirely

### 3. **Autogenerated API Docs**

- API reference files use module path as title
- Example: `haive.agents.base` instead of "Base Agents Module"
- This is standard Sphinx behavior but looks poor in sidebar

## Fixed Issues

### ✅ Noxfile Configuration

- Removed `-W` flag from default `docs` session
- Created separate `docs_strict` for warnings-as-errors
- Added `docs_minimal` for fastest builds
- Improved logging and error messages

### ✅ JavaScript/CSS Fixes

- `sidebar-fix.js` - Intelligently reformats module paths
- `sidebar-fix.css` - Improves nested navigation styling
- Disabled `showcase.js/css` that was overriding pages

## Recommendations

### 1. **Fix Missing Titles** (High Priority)

All `.rst` files need proper title formatting:

```rst
Core Concepts
=============

Content here...
```

### 2. **Create Missing Files**

- Create `/docs/source/beta_status.rst`
- Add proper titles to all guide files

### 3. **Improve API Docs**

Consider using custom templates for autogenerated API docs to have better titles

### 4. **Regular Monitoring**

- Check `/packages/logs/` for test failures
- Run `nox -s docs_check` for link validation
- Use `nox -s docs_strict` before releases

## Quick Commands

```bash
# View documentation issues
tail -100 /tmp/docs_fast.log | grep WARNING

# Check sidebar in browser
poetry run nox -s docs_view

# Rebuild with latest fixes
poetry run nox -s docs

# Check test logs
cat /packages/logs/pytest.log
```
