"""{{ module_name|replace('_', ' ')|title }} module.

{% if has_classes and has_functions %}
This module provides {{ class_count }} classes and {{ function_count }} utility functions
for {{ purpose|default('specialized processing') }}.
{% elif has_classes %}
This module defines {{ class_count }} class{{ 'es' if class_count > 1 else '' }} for {{ purpose|default('data management') }}.
{% elif has_functions %}
This module provides {{ function_count }} utility function{{ 's' if function_count > 1 else '' }} for {{ purpose|default('data processing') }}.
{% else %}
This module provides core functionality for the {{ package_name|default('application') }}.
{% endif %}

{% if main_classes %}
Classes:
{% for class in main_classes[:3] %}
    {{ class.name }}: {{ class.description|default('Core implementation class') }}.
{% endfor %}
{% if main_classes|length > 3 %}
    ... and {{ main_classes|length - 3 }} more classes.
{% endif %}
{% endif %}

{% if main_functions %}
Functions:
{% for func in main_functions[:3] %}
    {{ func.name }}(): {{ func.description|default('Utility function') }}.
{% endfor %}
{% if main_functions|length > 3 %}
    ... and {{ main_functions|length - 3 }} more functions.
{% endif %}
{% endif %}

{% if has_main %}
Usage:
    python -m {{ module_path }} [options]
    
    Run with --help for available options.
{% elif example_import %}
Example:
    >>> from {{ import_path|default(module_name) }} import {{ main_export|default('main') }}
    >>> result = {{ main_export|default('main') }}({{ example_args|default('data') }})
{% endif %}

{% if dependencies %}
Dependencies:
    - {{ dependencies|join('\n    - ') }}
{% endif %}

{% if see_also %}
See Also:
    {{ see_also }}
{% endif %}
"""