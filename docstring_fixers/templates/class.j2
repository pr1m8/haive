"""{{ class_description|default(class_name + ' implementation') }}.

{% if detailed_description %}
{{ detailed_description }}
{% elif base_classes %}
This class extends {{ base_classes|join(', ') }} to provide
{{ purpose|default('specialized functionality') }}.
{% else %}
This class provides {{ purpose|default('core functionality for ' + class_name|lower + ' operations') }}.
{% endif %}

{% if attributes %}
Attributes:
{% for attr in attributes %}
    {{ attr.name }}{% if attr.type %} ({{ attr.type }}){% endif %}: {{ attr.description }}.
        {%- if attr.default %} Default: {{ attr.default }}.{% endif %}
{% endfor %}
{% endif %}

{% if class_methods %}
Class Methods:
{% for method in class_methods[:5] %}
    {{ method.name }}(): {{ method.description|default('Class method') }}.
{% endfor %}
{% if class_methods|length > 5 %}
    ... and {{ class_methods|length - 5 }} more methods.
{% endif %}
{% endif %}

{% if properties %}
Properties:
{% for prop in properties %}
    {{ prop.name }}: {{ prop.description|default('Read-only property') }}.
{% endfor %}
{% endif %}

{% if examples %}
Examples:
    Basic usage:
    
    >>> {{ class_name|lower }} = {{ class_name }}({{ example_init_args|default('') }})
    >>> {{ class_name|lower }}.{{ example_method|default('process') }}({{ example_method_args|default('') }})
    {{ example_output|default('# Result...') }}
    
    {% if advanced_example %}
    Advanced usage:
    
    {{ advanced_example|indent(4) }}
    {% endif %}
{% endif %}

{% if inheritance_notes %}
Inheritance Notes:
    {{ inheritance_notes }}
{% endif %}

{% if see_also %}
See Also:
    {{ see_also }}
{% endif %}
"""