"""{{ method_description|default(method_name|replace('_', ' ')|capitalize) }}.

{% if is_property_setter %}
Set the {{ property_name }} property.
{% elif is_property_getter %}
Get the {{ property_name }} property.
{% elif is_magic_method %}
{{ magic_method_descriptions.get(method_name, 'Magic method implementation') }}.
{% elif detailed_description %}
{{ detailed_description }}
{% endif %}

{% if args %}
Args:
{% for arg in args %}
    {%- if arg.name != 'self' %}
    {{ arg.name }}{% if arg.annotation %} ({{ arg.annotation }}){% endif %}: {{ arg.description|default('The ' + arg.name|replace('_', ' ')) }}.
        {%- if arg.default %} Default: {{ arg.default }}.{% endif %}
        {%- if arg.valid_values %} Valid values: {{ arg.valid_values|join(', ') }}.{% endif %}
    {% endif %}
{% endfor %}
{% endif %}

{% if returns %}
Returns:
    {% if returns.annotation %}{{ returns.annotation }}: {% endif %}{% if method_name == '__init__' %}None.{% else %}{{ returns.description|default('The result') }}.{% endif %}
{% endif %}

{% if yields %}
Yields:
    {% if yields.annotation %}{{ yields.annotation }}: {% endif %}{{ yields.description }}.
{% endif %}

{% if raises %}
Raises:
{% for exc in raises %}
    {{ exc.type }}: {{ exc.description }}.
{% endfor %}
{% endif %}

{% if side_effects %}
Side Effects:
    {{ side_effects }}
{% endif %}

{% if examples %}
Examples:
    >>> obj.{{ method_name }}({{ example_args|default('') }})
    {{ example_output|default('...') }}
{% endif %}

{% if notes %}
Notes:
    {{ notes }}
{% endif %}
"""