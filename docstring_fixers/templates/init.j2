"""Initialize {{ class_name }} instance.

{% if class_description %}
{{ class_description }}
{% else %}
Creates a new {{ class_name }} with the specified configuration.
{% endif %}

{% if args %}
Args:
{% for arg in args %}
    {{ arg.name }}{% if arg.annotation %} ({{ arg.annotation }}){% endif %}: {% if arg.description %}{{ arg.description }}{% else %}The {{ arg.name|replace('_', ' ') }} parameter{% endif %}.
        {%- if arg.default %} Default: {{ arg.default }}.{% endif %}
        {%- if arg.constraints %} {{ arg.constraints }}.{% endif %}
{% endfor %}
{% endif %}

{% if attributes %}
Attributes:
{% for attr in attributes %}
    {{ attr.name }}: {{ attr.description|default('Instance attribute') }}.
{% endfor %}
{% endif %}

{% if raises %}
Raises:
{% for exc in raises %}
    {{ exc.type }}: {{ exc.description|default('If validation fails') }}.
{% endfor %}
{% endif %}

{% if examples %}
Examples:
    >>> {{ class_name|lower }} = {{ class_name }}({{ example_args|default('name="example"') }})
    >>> {{ class_name|lower }}.{{ example_method|default('process()') }}
    {{ example_output|default('# Output...') }}
{% endif %}

{% if notes %}
Notes:
    {{ notes }}
{% endif %}
"""