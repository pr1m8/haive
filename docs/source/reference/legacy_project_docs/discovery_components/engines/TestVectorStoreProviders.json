{
  "component_info": {
    "name": "TestVectorStoreProviders",
    "component_type": "vector_store",
    "module_path": "packages.haive-core.src.haive.core.engine.vectorstore.test_vectorstore_providers",
    "class_name": "TestVectorStoreProviders",
    "description": "Test case for vector store providers.",
    "source_code": "class TestVectorStoreProviders(unittest.TestCase):\n    \"\"\"Test case for vector store providers.\"\"\"\n\n    def test_provider_enum_values(self):\n        \"\"\"Test that all providers have proper string values.\"\"\"\n        # Verify all providers have non-empty string values\n        for provider in VectorStoreProvider:\n            self.assertIsInstance(provider.value, str)\n            self.assertTrue(provider.value)  # Non-empty string\n\n    def test_provider_registry(self):\n        \"\"\"Test that the provider registry returns classes for built-in providers.\"\"\"\n        # Test getting the class for a built-in provider\n        # Note: This doesn't actually instantiate the class\n        provider_class = VectorStoreProviderRegistry.get_provider_class(\n            VectorStoreProvider.FAISS\n        )\n        self.assertIsNone(\n            provider_class\n        )  # Should be None since we're falling back to the built-in imports\n\n    def test_register_custom_provider(self):\n        \"\"\"Test registering a custom provider.\"\"\"\n        # Create a mock VectorStore class\n        class MockVectorStore(VectorStore):\n            \"\"\"Mock vector store for testing.\"\"\"\n\n            def add_texts(\n                self, texts: List[str], metadatas=None, **kwargs\n            ):\n                \"\"\"Add texts to the vector store.\"\"\"\n                return [\"id1\", \"id2\", \"id3\"]\n\n            def similarity_search(self, query: str, k: int = 4, **kwargs):\n                \"\"\"Search for similar documents.\"\"\"\n                return []\n\n            @classmethod\n            def from_texts(\n                cls, texts: List[str], embedding, metadatas=None, **kwargs\n            ):\n                \"\"\"Create a vector store from texts.\"\"\"\n                return cls()\n\n            @classmethod\n            def from_documents(cls, documents, embedding, **kwargs):\n                \"\"\"Create a vector store from documents.\"\"\"\n                return cls()\n\n        # Register the custom provider\n        VectorStoreProviderRegistry.register_provider(\"MockStore\", MockVectorStore)\n\n        # Get the class from the registry\n        provider_class = VectorStoreProviderRegistry.get_provider_class(\"MockStore\")\n        self.assertEqual(provider_class, MockVectorStore)\n\n    def test_provider_factory(self):\n        \"\"\"Test registering a provider factory.\"\"\"\n        # Create a factory function\n        def get_mock_vectorstore() -> Type[VectorStore]:\n            \"\"\"Factory function returning a mock vector store class.\"\"\"\n\n            class FactoryMockVectorStore(VectorStore):\n                \"\"\"Mock vector store created from a factory.\"\"\"\n\n                def add_texts(\n                    self, texts: List[str], metadatas=None, **kwargs\n                ):\n                    \"\"\"Add texts to the vector store.\"\"\"\n                    return [\"id1\", \"id2\", \"id3\"]\n\n                def similarity_search(self, query: str, k: int = 4, **kwargs):\n                    \"\"\"Search for similar documents.\"\"\"\n                    return []\n\n                @classmethod\n                def from_texts(\n                    cls, texts: List[str], embedding, metadatas=None, **kwargs\n                ):\n                    \"\"\"Create a vector store from texts.\"\"\"\n                    return cls()\n\n                @classmethod\n                def from_documents(cls, documents, embedding, **kwargs):\n                    \"\"\"Create a vector store from documents.\"\"\"\n                    return cls()\n\n            return FactoryMockVectorStore\n\n        # Register the factory\n        VectorStoreProviderRegistry.register_provider_factory(\n            \"FactoryMockStore\", get_mock_vectorstore\n        )\n\n        # Get the class from the registry\n        provider_class = VectorStoreProviderRegistry.get_provider_class(\"FactoryMockStore\")\n        self.assertIsNotNone(provider_class)\n        self.assertTrue(issubclass(provider_class, VectorStore))\n\n    def test_list_providers(self):\n        \"\"\"Test listing all providers.\"\"\"\n        providers = VectorStoreProviderRegistry.list_providers()\n        \n        # Check that we have all the built-in providers\n        self.assertIn(\"Chroma\", providers)\n        self.assertIn(\"FAISS\", providers)\n        self.assertIn(\"Pinecone\", providers)\n        self.assertIn(\"Weaviate\", providers)\n        self.assertIn(\"PGVector\", providers)\n        self.assertIn(\"Redis\", providers)\n        self.assertIn(\"Elasticsearch\", providers)\n        \n        # Note: We don't check for custom providers here as they may not \n        # be properly registered due to enum extension limitations\n",
    "env_vars": [],
    "timestamp": "2025-06-19T12:31:29.581522",
    "schema": {
      "properties": {
        "methodName": {
          "type": "Any",
          "default": "runTest"
        }
      }
    },
    "metadata": {},
    "has_tool": true,
    "tool_name": "search_testvectorstoreproviders",
    "tool_description": "Search documents using TestVectorStoreProviders",
    "engine_config": {
      "engine_type": "vector_store",
      "vector_store_class": "TestVectorStoreProviders",
      "module_path": "packages.haive-core.src.haive.core.engine.vectorstore.test_vectorstore_providers",
      "description": "Test case for vector store providers.",
      "env_vars": [],
      "schema": {
        "properties": {
          "methodName": {
            "type": "Any",
            "default": "runTest"
          }
        }
      }
    }
  },
  "document": {
    "page_content": "# TestVectorStoreProviders\n**Type:** vector_store\n**Module:** packages.haive-core.src.haive.core.engine.vectorstore.test_vectorstore_providers\n**Class:** TestVectorStoreProviders\n\n## Description\nTest case for vector store providers.\n\n## Schema\n```json\n{\n  \"properties\": {\n    \"methodName\": {\n      \"type\": \"Any\",\n      \"default\": \"runTest\"\n    }\n  }\n}\n```\n\n## Usage as Tool\n**Tool Name:** `search_testvectorstoreproviders`\n**Description:** Search documents using TestVectorStoreProviders\n\n## Engine Configuration\nThis component can be used as a Haive engine:\n```python\n# Example engine usage\nengine = TestVectorStoreProvidersEngine(name='TestVectorStoreProviders')\n```\n\n## Source Code\n```python\nclass TestVectorStoreProviders(unittest.TestCase):\n    \"\"\"Test case for vector store providers.\"\"\"\n\n    def test_provider_enum_values(self):\n        \"\"\"Test that all providers have proper string values.\"\"\"\n        # Verify all providers have non-empty string values\n        for provider in VectorStoreProvider:\n            self.assertIsInstance(provider.value, str)\n            self.assertTrue(provider.value)  # Non-empty string\n\n    def test_provider_registry(self):\n        \"\"\"Test that the provider registry returns classes for built-in providers.\"\"\"\n        # Test getting the class for a built-in provider\n        # Note: This doesn't actually instantiate the class\n        provider_class = VectorStoreProviderRegistry.get_provider_class(\n            VectorStoreProvider.FAISS\n        )\n        self.assertIsNone(\n            provider_class\n        )  # Should be None since we're falling back to the built-in imports\n\n    def test_register_custom_provider(self):\n        \"\"\"Test registering a custom provider.\"\"\"\n        # Create a mock VectorStore class\n        class MockVectorStore(VectorStore):\n            \"\"\"Mock vector store for testing.\"\"\"\n\n            def add_texts(\n                self, texts: List[str], metadatas=None, **kwargs\n            ):\n                \"\"\"Add texts to the vector store.\"\"\"\n                return [\"id1\", \"id2\", \"id3\"]\n\n            def similarity_search(self, query: str, k: int = 4, **kwargs):\n                \"\"\"Search for similar documents.\"\"\"\n                return []\n\n            @classmethod\n            def from_texts(\n                cls, texts: List[str], embedding, metadatas=None, **kwargs\n            ):\n                \"\"\"Create a vector store from texts.\"\"\"\n                return cls()\n\n            @classmethod\n            def from_documents(cls, documents, embedding, **kwargs):\n                \"\"\"Create a vector store from documents.\"\"\"\n                return cls()\n\n        # Register the custom provider\n        VectorStoreProviderRegistry.register_provider(\"MockStore\", MockVectorStore)\n\n        # Get the class from the registry\n        provider_class = VectorStoreProviderRegistry.get_provider_class(\"MockStore\")\n        self.assertEqual(provider_class, MockVectorStore)\n\n    def test_provider_factory(self):\n        \"\"\"Test registering a provider factory.\"\"\"\n        # Create a factory function\n        def get_mock_vectorstore() -> Type[VectorStore]:\n            \"\"\"Factory function returning a mock vector store class.\"\"\"\n\n            class FactoryMockVectorStore(VectorStore):\n                \"\"\"Mock vector store created from a factory.\"\"\"\n\n                def add_texts(\n                    self, texts: List[str], metadatas=None, **kwargs\n                ):\n                    \"\"\"Add texts to the vector store.\"\"\"\n                    return [\"id1\", \"id2\", \"id3\"]\n\n                def similarity_search(self, query: str, k: int = 4, **kwargs):\n                    \"\"\"Search for similar documents.\"\"\"\n                    return []\n\n                @classmethod\n                def from_texts(\n                    cls, texts: List[str], embedding, metadatas=None, **kwargs\n                ):\n                    \"\"\"Create a vector store from texts.\"\"\"\n                    return cls()\n\n                @classmethod\n                def from_documents(cls, documents, embedding, **kwargs):\n                    \"\"\"Create a vector store from documents.\"\"\"\n                    return cls()\n\n            return FactoryMockVectorStore\n\n        # Register the factory\n        VectorStoreProviderRegistry.register_provider_factory(\n            \"FactoryMockStore\", get_mock_vectorstore\n        )\n\n        # Get the class from the registry\n        provider_class = VectorStoreProviderRegistry.get_provider_class(\"FactoryMockStore\")\n        self.assertIsNotNone(provider_class)\n        self.assertTrue(issubclass(provider_class, VectorStore))\n\n    def test_list_providers(self):\n        \"\"\"Test listing all providers.\"\"\"\n        providers = VectorStoreProviderRegistry.list_providers()\n        \n        # Check that we have all the built-in providers\n        self.assertIn(\"Chroma\", providers)\n        self.assertIn(\"FAISS\", providers)\n        self.assertIn(\"Pinecone\", providers)\n        self.assertIn(\"Weaviate\", providers)\n        self.assertIn(\"PGVector\", providers)\n        self.assertIn(\"Redis\", providers)\n        self.assertIn(\"Elasticsearch\", providers)\n        \n        # Note: We don't check for custom providers here as they may not \n        # be properly registered due to enum extension limitations\n\n```",
    "metadata": {
      "name": "TestVectorStoreProviders",
      "component_type": "vector_store",
      "module_path": "packages.haive-core.src.haive.core.engine.vectorstore.test_vectorstore_providers",
      "class_name": "TestVectorStoreProviders",
      "has_tool": true,
      "has_engine_config": true,
      "env_vars": [],
      "timestamp": "2025-06-19T12:31:29.581522",
      "category": "engines"
    }
  },
  "tool_available": true,
  "engine_config_available": true
}
