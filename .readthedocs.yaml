version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

python:
  install:
    - method: poetry
      path: .  # This assumes your root pyproject.toml defines all deps

sphinx:
  configuration: docs/source/conf.py
